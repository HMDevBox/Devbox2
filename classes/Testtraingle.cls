public class Testtraingle{
public static testmethod void method(){
triangle first=new triangle();
first.setvalues(10,5);
first.getvalues();
integer ar=first.area();
system.debug('area of traiangle is'+ ar);
}
}